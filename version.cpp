// ═══════════════════════════════════════════════════════════════════════════════
//  IPView Pro v2.15.5 — version.cpp
//  Translation unit anchor for the auto-generated version.hpp.
//
//  All consteval accessors (appName, appVersion, appOrgName, appOrgDomain,
//  appDisplayName) are defined in version.hpp itself so the compiler can
//  fold them at any call site. This .cpp exists only to guarantee the
//  generated header is part of the build graph and to host a small set
//  of run-time helpers (currently none) that may grow over time.
//
//  Public Domain — No License — No Restrictions.
// ═══════════════════════════════════════════════════════════════════════════════

#include "version.hpp"

// Intentionally empty — see header for the consteval accessors.
